#include <stdio.h>
#include <stdlib.h>

int main()
{
    int counter, max, min, program_guessed_number, userinput;
    scanf("%d", &min);
    scanf("%d", &max);

    while (userinput == 3)
    {
        printf("1. TOO LARGE\n 2. TOO SMALL\n 3. FOUND\n");
        scanf("%d" &userinput);
    }
}